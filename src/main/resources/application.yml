server:
  port: 8080
app:
  address: http://localhost:8080

encryption:
  key: 18'XY5W^:@LhW^V8NiEUftJ@+jd|LWOS
  algorithm: AES
  transformation: AES/CBC/PKCS5Padding

spring:
  mail:
    host: smtp.yandex.ru
    port: 465
    username: program-shcool@yandex.ru
    password: jlkkhlccvtkygrgn
    protocol: smtps
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          starttls:
            enable: false

  task:
    execution:
      pool:
        core-size: 5
        max-size: 10

  data:
    redis:
      host: localhost
      port: 6379

  application:
    name: quizizz

  datasource:
    url: jdbc:postgresql://localhost:6432/quizizz
    username: user
    password: password
    driverClassName: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

  sql:
    init:
      mode: always

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.transaction: DEBUG
    org.springframework.orm.jpa: DEBUG